# frontend/.env.example
# Copy to frontend/.env and set the backend URL

# Local development (backend running on port 5000)
VITE_API_URL=http://localhost:5000/api

# Production (after deploying backend to Railway/Render)
# VITE_API_URL=https://your-nexus-backend.railway.app/api
